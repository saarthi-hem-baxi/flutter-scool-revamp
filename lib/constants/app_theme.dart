import 'package:flutter/material.dart';

/// App theme related stuff will be done here
class AppTheme {
  static Size designSize = const Size(414, 736); // Define size as per the design screen of your XD
}
